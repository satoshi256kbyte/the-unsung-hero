import type { EventDefinition } from "./index.js";

/** ひらめきイベントの基本発生確率（未実装のため未使用） */
export const BASE_PROB = 0.03;

export const inspiration: EventDefinition = {
  roll() {
    return null;
  },
};
