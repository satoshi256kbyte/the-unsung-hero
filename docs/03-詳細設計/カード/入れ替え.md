# 入れ替え

| 項目 | 内容 |
| ---- | ---- |
| 効果 | 要検討（現時点で未設計） |
| 適用方式 | 要検討 |
| コスト（仮） | 要検討 |

現時点では `CardName` として型定義のみ存在し、具体的な効果・適用方式は未設計。
名称から「カード枠の入れ替え」に関連する挙動が想定されるが、
[カード選択UI設計](../../superpowers/specs/2026-08-10-card-selection-ui-design.md) の
ドラッグ＆ドロップによる入れ替え操作そのものとは別に、独立したカード効果として
何を持たせるかは今後検討する。
